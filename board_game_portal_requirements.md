# ボードゲームポータルサイト 要求仕様書

対象読者: この仕様書を読んで実装するAIコーディングエージェント（Claude Code）向け。
発注者（プロジェクトリーダー）はコーディングを行わないため、実装の詳細判断（変数名・ファイル分割・具体的なアルゴリズム実装等）はエージェント側の裁量に委ねる。ただし本書に明記した**ルール・データモデル・UI要件は仕様として厳守**すること。

---

## 0. この仕様書の前提・進め方

- 本サイトは複数のミニボードゲームを1つのポータルにまとめ、CPU対戦・将来的にオンライン対戦もできるようにする長期プロジェクトの**第1弾**。
- 今回のスコープは **「五目並べ」「五五将棋」の2ゲームをCPU対戦で遊べるポータルサイトを作ること**。オンライン対戦（Firebase Firestore使用）は次フェーズとする。
- 五目並べには単体サイト向けに書かれた既存仕様書（`gomoku_requirements.md`、本書末尾に全文再掲）が存在する。**既存実装コードがあればそれを可能な限り流用**し、ポータルの共通UI・ゲーム選択の仕組みに合わせて組み込むこと。既存コードの流用可否・移植方法はエージェントの判断でよい。
- 本書内の「未確定・要相談」と書かれた項目以外は、記載内容に沿って実装を進めてよい。判断に迷った場合はREADME等にTODOとして残し、都度確認を求めること。

---

## 1. 全体概要

| 項目 | 内容 |
|---|---|
| サイト名 | 仮称: ボードゲームファーム（リポジトリ名 `board-game-farm` に準拠。正式名称は後日変更の可能性あり） |
| リポジトリ | `git@github.com:ysk423/board-game-farm.git` |
| ホスティング | GitHub Pages（静的サイト、上記リポジトリの `gh-pages` またはリポジトリ設定に応じたブランチから配信） |
| バックエンド | 今回のスコープでは不要（CPU対戦のみのため）。将来のオンライン対戦フェーズでFirebase Firestoreを追加 |
| 技術スタック | TypeScript + Vite（他プロジェクトと統一） |
| パッケージマネージャ | npm |
| Git運用 | mainブランチに直接コミット・プッシュしてよい（作業用ブランチの分離は不要） |
| 対応ゲーム（今回） | ①五目並べ（Gomoku） ②五五将棋（ごごしょうぎ） |
| 対戦モード（今回） | CPU対戦のみ（難易度: 弱・中・強の3段階） |
| 対戦モード（将来） | オンライン対戦（ルームURL共有方式、五目並べの既存仕様を踏襲） |
| 認証 | なし（名前入力のみ、または匿名でも可） |

---

## 2. 全体アーキテクチャ

### 2.1 サイト構成方針

- **1つのポータルサイトに複数ゲームをまとめる**（共通UI・将来は共通DB）。
- ポータルのトップページにゲーム一覧（カード形式）を表示し、ゲームを選ぶと各ゲーム画面に遷移する構成とする。
- 例: `https://ysk423.github.io/board-game-farm/` がトップ、`.../gomoku/` `.../gogo-shogi/` が各ゲーム画面（Vite Multi-Page ApplicationまたはSPA内ルーティング、実装方法はエージェントの判断でよい）。

### 2.2 開発フェーズ

| フェーズ | 内容 |
|---|---|
| **Phase 1（今回のスコープ）** | ポータルの土台（トップページ・ゲーム選択UI・共通デザイン） + 五目並べCPU対戦 + 五五将棋CPU対戦 |
| Phase 2（将来） | オンライン対戦をポータル全体に追加（Firebase Firestore導入、五目並べの既存オンライン仕様をベースに五五将棋にも展開） |
| Phase 3（将来） | 新規ゲームの追加（都度個別に要件定義予定） |

Phase 1では**Firebaseは導入しない**（完全に静的なサイトとしてGitHub Pagesのみで完結させる）。Phase 2着手時に改めてFirestoreのデータモデルを設計する。

### 2.3 ディレクトリ構成（提案）

以下は一例。エージェントの判断で調整してよいが、「ゲームごとにロジックを分離し、共通部分をshared化する」という方針は維持すること。

```
/
├─ index.html                 # ポータルトップ（ゲーム選択画面）
├─ src/
│  ├─ portal/                 # トップページ・ゲーム選択UI
│  ├─ shared/                 # 共通コンポーネント・共通スタイル・難易度選択UIなど
│  ├─ games/
│  │  ├─ gomoku/               # 五目並べ（既存実装を移植）
│  │  │  ├─ logic/             # 盤面ロジック・勝敗判定・CPU AI
│  │  │  └─ ui/
│  │  └─ gogo-shogi/            # 五五将棋
│  │     ├─ logic/
│  │     └─ ui/
│  └─ types/
├─ gomoku.html                 # 例: Multi-page構成の場合
├─ gogo-shogi.html
└─ vite.config.ts
```

---

## 3. 共通仕様（ポータル基盤）

### 3.1 トップ画面（ゲーム選択）

- ゲームをカード形式で一覧表示（サムネイル/アイコン、ゲーム名、簡単な説明）。
- カードをクリックすると該当ゲームの画面に遷移。
- デザインテイストは**新規に検討**（過去のインディゴブルー系カードデザインの流用は不要）。方向性はエージェント側で2〜3案提示してもよい。落ち着いた・遊び心のある、どちらの方向でもよいが、複数ゲームを横断して統一感のあるデザインシステム（配色・カード形状・フォント）にすること。
- 今後ゲームが増えていくことを前提に、カード追加が容易な構造にする。

### 3.2 難易度選択の共通UI仕様

- 各ゲームで対局開始前に **弱 / 中 / 強** の3段階から難易度を選択するUIを表示する。
- 見た目・操作感は両ゲームで共通コンポーネント化する。
- 難易度ごとのAI実装方針は各ゲームのセクションに記載。

### 3.3 共通コンポーネント（例）

- 難易度選択UI
- 対局結果表示（勝ち/負け/引き分けのモーダルまたはメッセージ）
- 「もう一度対局する」ボタン（トップ画面や難易度選択に戻る）
- ヘッダー（ポータルトップに戻る導線）

---

## 4. ゲーム1: 五目並べ（Gomoku）

### 4.1 既存仕様との関係

既存の `gomoku_requirements.md`（本書末尾に全文添付）は「単体サイト・オンライン対戦のみ」を前提に書かれたもの。今回のポータル化にあたり、以下の差分を適用する。

| 項目 | 既存仕様（単体サイト） | 今回追加・変更 |
|---|---|---|
| 対戦モード | オンライン対戦のみ | **CPU対戦を新規追加**（Phase 1のメイン）。オンライン対戦はPhase 2でポータルに組み込む |
| ルール（盤面・勝敗判定） | 15×15、5つ以上連続で勝利、禁じ手なし | 変更なし。CPU対戦でもこのルールをそのまま使う |
| UI | 単体サイトのトップ〜対局画面 | ポータル共通のデザイン・ヘッダー・難易度選択UIに合わせて調整 |
| データ永続化 | Firestore（オンライン対戦用） | Phase 1ではCPU対戦のみなのでFirestore不要。対局状態はブラウザ内（メモリ/State）で完結 |

既存実装コードがある場合、盤面ロジック・勝敗判定ロジックはそのまま流用可能なはず（オンライン対戦用の通信部分だけ今回は使わない）。移植の可否・手順はエージェントの判断に委ねる。

### 4.2 CPU対戦仕様（新規）

- プレイヤーは黒（先手）固定でよい（CPUが常に白/後手）。
- 難易度ごとのAI方針:

| 難易度 | 実装方針の目安 |
|---|---|
| 弱 | ランダムに近い手。ただし「自分が置けば5つ揃う手」があれば必ず打つ、「相手が次に置けば5つ揃ってしまう手」があれば阻止する、程度の最低限の防御は入れる |
| 中 | 簡単な評価関数（連続数・両端の空き具合などをスコア化）による1〜2手先読み、または軽量なミニマックス |
| 強 | 評価関数 + ミニマックス法（αβ枝刈り推奨）で数手先読み。着手候補を盤面中央付近・既存の石の周辺に絞るなどして現実的な速度で動作させる |

- 具体的な評価関数の設計・探索深さの数値はエージェントの裁量で調整してよい（ブラウザ上で1手あたり数秒以内に応答できることを目安とする）。
- CPU対戦では既存仕様にあった「ルーム作成・共有」「切断時の挙動」等のオンライン特有の要件は不要。

### 4.3 オンライン対戦（Phase 2・参考情報）

Phase 2着手時、既存の `gomoku_requirements.md` の第3〜6章（ルーム作成/参加フロー、勝敗判定、Firestoreデータモデル、Security Rules）をベースに、ポータル共通のFirebaseプロジェクトに統合する形で再設計する。今回は実装不要、参考情報として残す。

---

## 5. ゲーム2: 五五将棋（ごごしょうぎ / Minishogi）

### 5.1 概要

5×5マスの盤で行う将棋のミニチュア版。1970年に楠本茂信が考案した一般的な「5五将棋」ルールを採用する。桂馬・香車は使用しない。

### 5.2 ルール仕様

**盤面・駒**

- 盤面: 5×5マス。
- 使用する駒（各プレイヤー1枚ずつ、計6種12枚）: 王将（先手）/玉将（後手）、飛車、角行、金将、銀将、歩兵。
- 駒の動き方は本将棋と同じ（王: 全方向1マス、飛車: 縦横に何マスでも、角行: 斜めに何マスでも、金将: 前・斜め前・横・後の6方向に1マス、銀将: 前・斜め前・斜め後の4方向に1マス、歩兵: 前に1マス）。

**初期配置**

先手（下段、5段目）、ファイル1〜5（左から右）:

| 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|
| 飛車 | 角行 | 銀将 | 金将 | 王将 |

先手の歩兵は王将の前（5筋4段目）に1枚のみ。

後手（上段、1段目）は先手の配置を180度回転した形（点対称）:

| 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|
| 玉将 | 金将 | 銀将 | 角行 | 飛車 |

後手の歩兵は玉将の前（1筋2段目）に1枚のみ。

**自陣・敵陣と成り**

- 敵陣は最も奥の1列のみ（本将棋は3列だが、五五将棋では1列）。
- 敵陣に入る手、または敵陣から動く手を指した際に、その駒を「成る」ことができる（成るかどうかは任意選択、本将棋と同様）。
- 王将・玉将・金将は成れない。飛車→竜王、角行→竜馬、銀将→成銀、歩兵→と金。

**禁止事項（本将棋と同様）**

- 二歩（同じ筋に自分の歩兵が2枚になる手）は禁止。
- 打ち歩詰め（歩を打って詰ませる手）は禁止。
- 行き所のない駒を打つ・進める手は禁止（例: 最奥段への歩の着地で次に動けなくなる場合など）。

**勝敗判定**

- 相手の玉（王）を詰ませたら勝利（通常の将棋と同じ「詰み」の判定）。
- 投了機能を用意し、CPU側が明らかに不利と判断した場合は投了させてもよい（実装はエージェント裁量）。
- **千日手（同一局面が4回出現）は先手の負け**とする（五五将棋の慣例ルール。後手有利になるよう調整するための特別ルール）。連続王手の千日手（王手をかけ続けた側が反則負けになるケース）は本将棋と同様に王手をかけ続けた側の負けとする。
- 持将棋（お互いに詰ませられない停滞状態）は今回のスコープでは判定ロジックを実装しない。膠着した場合は投了ボタンでの終局を促す表示を出す程度でよい。

### 5.3 CPU対戦仕様

- プレイヤーは先手（王将）固定でよい（CPUが後手/玉将）。
- 難易度ごとのAI方針（五目並べと同様の考え方）:

| 難易度 | 実装方針の目安 |
|---|---|
| 弱 | 合法手からランダムに選択。ただし「王手放置」「自玉が即座に取られる手」は避ける |
| 中 | 駒得（取った駒の価値）＋簡単な玉の安全度評価による、2〜3手程度のミニマックス探索 |
| 強 | 駒得＋盤面支配・玉の危険度などを含めた評価関数によるミニマックス（αβ枝刈り推奨）、4〜6手程度先読み |

- 盤面が小さい（5×5）ため、ブラウザ上でも十分な深さの探索が現実的に可能。具体的な評価関数の重み付けや探索深さの数値はエージェントの裁量で調整してよい。

### 5.4 画面構成

- 五目並べと共通のレイアウト思想（難易度選択→対局画面→結果表示）を踏襲する。
- 対局画面: 5×5の盤面、持ち駒表示（取った相手の駒を自分の持ち駒として打てるルールのため）、手番表示、成り選択のポップアップ（敵陣に入る手を指した際）。
- 駒の表示は文字（例: 「王」「飛」「角」等）またはシンプルな駒アイコンのどちらでもよい（エージェント裁量、五目並べと統一感のあるデザインで）。

### 5.5 データモデル（Phase 2・参考情報）

Phase 1ではFirestore不要（ブラウザ内Stateで完結）。Phase 2でオンライン対戦を追加する際は、五目並べのFirestore設計（`games/{roomId}` コレクション）を踏襲し、盤面・持ち駒・手番・成り状態などを含む形でデータモデルを設計し直す。今回は実装不要。

---

## 6. 今後の拡張候補（参考メモ）

- Phase 2: オンライン対戦をポータル全体に導入（Firebase Firestore）
- Phase 3以降: 新規ゲームの追加（都度個別要件定義）
- 対局結果の記録・戦績表示（要認証、スコープ外）
- ランキング機能（スコープ外）

---

## 7. Claude Codeへの実装依頼時の進め方（補足）

- まずポータルの土台（トップページ・共通コンポーネント・ディレクトリ構成）を作り、その上に五目並べ→五五将棋の順で組み込んでいく進め方を推奨する（発注者は非エンジニアのプロジェクトリーダーのため、動くものを都度確認しながら進めたい）。
- 五目並べの既存実装コードを流用する場合は、まずその実装をどう移植・改修するかの方針（ファイル構成・変更箇所）を簡潔に提示してから着手すること。
- 大きな設計判断（例: SPAにするかMulti-Page Applicationにするか等）で複数の選択肢がある場合は、実装前に選択肢と推奨案を提示すること。

---

## 付録: 既存仕様書 `gomoku_requirements.md`（全文・参考）

以下、単体サイト向けに書かれた既存仕様書の全文。4章で述べた差分を踏まえた上で参照すること。

> # オンライン五目並べ 要求仕様書
>
> ## 1. 概要
>
> GitHub Pagesでホストする、Firebase(Firestore)を使ったブラウザベースのオンライン五目並べ。
> ルームURLを友人と共有して1対1で対戦する、シンプルな2人用対戦ゲーム。
>
> - ホスティング: GitHub Pages（静的サイト）
> - バックエンド: Firebase Firestore（リアルタイム同期用。サーバーサイドロジックは持たない）
> - 技術スタック: Vanilla JS / HTML / CSS（軽量・依存少なめで実装しやすいため採用。フレームワーク不使用）
> - 認証: なし（名前入力のみ）
>
> （※本ポータル化にあたり、CPU対戦を新規追加。技術スタックはTypeScript + Viteに統一する点に注意。詳細は本書4章参照）
>
> ## 2. 対象外（スコープ外・単体サイト仕様当時のもの）
>
> - ランダムマッチング（今回はルームURL共有方式のみ）
> - 連珠ルール（禁じ手判定）→ 標準五目並べ（禁じ手なし、5つ以上並べば勝ち）
> - ユーザー認証（Googleログイン等）
> - チャット機能
> - 手番制限時間（タイマー）
> - リマッチ機能
> - 観戦機能
> - ランキング・戦績保存
>
> ## 3. 機能要件（オンライン対戦・Phase 2で再利用予定）
>
> ### 3.1 ルーム作成・参加フロー
>
> 1. トップ画面で名前を入力し「ルームを作成」ボタンを押すと、新しいルーム（ゲームドキュメント）がFirestoreに作成される
> 2. ルーム作成者は自動的に **黒（先手）** になる
> 3. 作成後、対戦相手に共有するための **ルームURL**（例: `https://xxx.github.io/gomoku/?room=abc123`）が画面に表示される（コピーボタン推奨）
> 4. 対戦相手がそのURLを開き、名前を入力して「参加」すると **白（後手）** としてルームに入る
> 5. 2人揃った時点でゲーム開始（対局中ステータスに遷移）
> 6. 3人目が同じURLでアクセスした場合は「このルームは満員です」と表示し参加不可とする
>
> ### 3.2 ゲームプレイ
>
> - 盤面サイズ: **15×15**
> - 交互に石を置く。自分の手番でのみ盤面クリック（タップ）操作が有効
> - 相手の手番中は盤面操作不可、「相手の手番です」等の待機表示を出す
> - 一度置いた石は動かせない・上書き不可
> - 現在の手番、プレイヤー名（黒/白）を常に画面に表示
>
> ### 3.3 勝敗判定
>
> - 縦・横・斜め（2方向）のいずれかで **同色の石が5つ以上連続** したら勝利（禁じ手なし、長連も勝ちとする）
> - 盤面が全て埋まり勝者がいない場合は「引き分け」
> - 勝敗確定後、盤面上に結果（勝者名 or 引き分け）を表示する。それ以上の操作（リマッチ等）はなし
>
> ### 3.4 切断時の挙動
>
> - 相手がブラウザを閉じる等で切断しても、自動的に不成立・引き分け扱いにはしない
> - 単純に「相手の応答を待つ」状態を維持する（タイムアウト処理は実装しない）
>
> ### 3.5 リロード時の挙動
>
> - ルームURLには `room=<roomId>` のパラメータを含める
> - プレイヤーが自分のブラウザをリロードしても、URLに含まれる `roomId` を元にFirestoreから現在の盤面・手番・プレイヤー情報を再取得し、同じ対局状態を復元する
>
> ## 4. データモデル（Firestore構造案・Phase 2で再設計予定）
>
> コレクション: `games`
> ドキュメントID: `roomId`（ランダム生成、例: 6〜8文字の英数字）
>
> ```
> games/{roomId}
> {
>   board: Array<Array<0|1|2>>,   // 15x15, 0=空, 1=黒, 2=白
>   turn: "black" | "white",       // 現在の手番
>   players: {
>     black: { name: string } | null,
>     white: { name: string } | null
>   },
>   status: "waiting" | "playing" | "finished",
>   winner: "black" | "white" | "draw" | null,
>   createdAt: Timestamp
> }
> ```
>
> ## 5. 画面構成（単体サイト当時のもの・ポータル化にあたり4章の差分を適用）
>
> ### 5.1 トップ画面
> - 名前入力欄
> - 「ルームを作成」ボタン
>
> ### 5.2 ルーム参加画面（URLに`room`パラメータがある場合）
> - 名前入力欄
> - 「参加する」ボタン
> - （満員の場合はエラーメッセージ表示）
>
> ### 5.3 対局画面
> - 15×15の盤面（グリッド、クリック/タップで着手）
> - 手番表示（自分の番か相手の番か）
> - 黒/白それぞれのプレイヤー名表示
> - ルームURL表示・コピー機能（対戦相手待ち中に表示）
> - 勝敗確定時のメッセージ表示（勝者 or 引き分け）
>
> ## 6. セキュリティに関する留意事項
>
> - Firebase設定（APIキー等）はクライアント側に公開される想定（Firebaseの一般的な仕様であり、それ自体は問題ない）
> - ただし誰でもFirestoreの`games`コレクションを自由に読み書きできる状態は避けたいため、Firestore Security Rulesで以下を最低限担保する:
>   - 既存ドキュメントの `board` は「現在の手番のプレイヤーのみ」更新可能、というロジックはクライアントでは強制できないため、可能な範囲でルールを設定（本格的な不正防止はスコープ外とする）
>   - 認証なしのため、性善説ベースの簡易ルールとする旨をREADME等に明記
